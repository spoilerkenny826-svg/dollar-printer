function startTrading() {
  alert('Trading dashboard coming next.');
}

function viewMarkets() {
  document.getElementById('dashboard').scrollIntoView({
    behavior: 'smooth',
    block: 'start',
  });
}
